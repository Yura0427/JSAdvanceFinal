import { FilSpeedsPipe } from './fil-speeds.pipe';

describe('FilSpeedsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilSpeedsPipe();
    expect(pipe).toBeTruthy();
  });
});
