import { SearchKeyPipe } from './search-key.pipe';

describe('SearchKeyPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchKeyPipe();
    expect(pipe).toBeTruthy();
  });
});
